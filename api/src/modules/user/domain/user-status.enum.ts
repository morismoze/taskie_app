export enum UserStatus {
  ACTIVE = 'ACTIVE',
  // Not yet used - maybe in the future if we
  // enable plain email/password registration
  // in which we will have email confirmation.
  INACTIVE = 'INACTIVE',
}
